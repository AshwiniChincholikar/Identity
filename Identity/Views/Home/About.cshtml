

@model IEnumerable<Identity.ViewModels.JoiningDateGroup>

@{
    ViewBag.Title = "User Statistics";
}

<h2>User Statistics</h2>

<table>
    <tr>
        <th>
            Date Of Joining
        </th>
        <th>
            Users
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfJoining)
            </td>
            <td>
                @item.UserCount
            </td>
        </tr>
    }
</table>